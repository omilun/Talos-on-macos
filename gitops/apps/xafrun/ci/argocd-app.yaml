# ArgoCD Application — xafrun-ci
# Deploys the Argo Workflows CI pipeline resources (WorkflowTemplates, EventSource, Sensors)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: xafrun-ci
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: apps
  source:
    repoURL: https://github.com/omilun/Talos-on-macos
    targetRevision: main
    path: gitops/apps/xafrun/ci
  destination:
    server: https://kubernetes.default.svc
    namespace: argo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
