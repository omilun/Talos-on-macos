# apps/kustomization.yaml
# User applications managed by ArgoCD (app-of-apps pattern) or Flux HelmReleases.
# This directory is the Day-2 application layer — add your own apps here.
#
# Future: add your own Helm charts from https://github.com/omilun/helm-charts
# and reference them with a HelmRepository + HelmRelease in this directory.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# Add your application manifests or sub-directories here, e.g.:
#   - my-app/
#   - another-app.yaml
